---
interface Props {
	tags: string[];
	title: string;
	desc: string;
	link: string;
}

const { tags, title, desc, link } = Astro.props;
---

<div class="bg-ldark md:w-80 rounded-md p-4 w-full">
	<a href={link}>
		<h3 class="text-white font-mono">{title}</h3>
		<p class="text-base">{desc}</p>
		<div class="flex mt-1 gap-1">
			{
			tags.map((tag, idx) => (
			<span class="font-mono text-dblue">{tag}</span>
			<span>{idx < tags.length - 1 ? "⋅" : ""}</span>
			))
			}
		</div>
	</a>
</div>
